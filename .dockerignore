# --- Monorepo essentials ---
**/node_modules
**/dist
**/build
**/.turbo
**/.cache
**/*.tsbuildinfo

# Git
.git
.gitignore

# Env files: ignore everything by default, then allow the prod ones we actually copy
.env
.env.*
.env.local
.env.*.local
!.env.example
!apps/frontend/.env.production
!apps/cms/.env.production
!apps/backend/.env.production

# Editors / OS junk
.vscode
.idea
.DS_Store
Thumbs.db

# Logs / temp / coverage
*.log
*.tmp
*.temp
coverage
.nyc_output

# Framework outputs
.keystone
.next
.remix

# App artifacts / uploads (don’t send heavy local files to build context)
**/uploads
**/public/storage
**/tmp

# ⚠ Do NOT ignore Docker files; they’re needed for build context
# .dockerignore
# Dockerfile*
# docker-compose*
